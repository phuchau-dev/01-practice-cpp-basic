#include "Addition.h"

void Addition::main() {
    int x = 5;
    int y = 5;
    int result = x + y;

    // Phép cộng
    std::cout << "Bài tập Addition cách 1 x là 5, y là 5, kết quả x + y là: " << (x + y) << std::endl;

    // Gán biến
    std::cout << "Bài tập Addition cách 2 x là 5, y là 5, kết quả x + y là: " << result << std::endl;
}
