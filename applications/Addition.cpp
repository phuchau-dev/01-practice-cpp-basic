#include "Addition.h"

void Addition::main() {
    int x = 5;
    int y = 5;
    int result = x + y;

    // Phép cộng
    std::cout << "Bai tap Addition cach 1 x là 5, y là 5, KET QUA x + y LA: " << (x + y) << std::endl;

    // Gán biến
    std::cout << "Bai tap Addition cach 2 x là 5, y là 5, KET QUA x + y LA: " << result << std::endl;
}
